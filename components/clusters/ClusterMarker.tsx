import React from "react";
import { Text, Cluster } from '../../shared'



const ClusterMarker = ({ count }) => (

    < Cluster >
        <Text>{count}</Text>
    </Cluster >

);

export default ClusterMarker;
